{
  "$comment": "**This mod was declared once, withdrawn before it shipped, and has come back a different shape. Read this before adding a setting to it.** The withdrawn draft had four settings — `swing`, `block_hit`, `always_swing` and `use_while_digging` — and it was pulled because the 1.7 half of every one of them was undefined from inside this repo: two of the four defaulted to `one_seven`, so shipping the schema without the mixins would have been a mod that turns on and changes nothing, which is the failure `text_shadow` was removed for on the day it landed. That gap is now closed the only way it could be — by disassembly, of Loom's named 1.8.9 jar and of a 1.7.10 jar remapped with Legacy Fabric yarn `1.7.10+build.603`, method against method. Three of the four settings did not survive contact with it, and this file is what is left.\n\n**`swing` does not exist, and must never be added back.** `LivingEntity.swingHand`, `tickHandSwing`, `getHandSwingProgress` and the private `getMiningSpeedMultiplier` they read are byte-for-byte identical across the two versions. So is the first-person arc: 1.8.9's `HeldItemRenderer.applyEquipAndSwingOffset` opens `GlStateManager.translate(0.56f, -0.52f, -0.71999997f)`, and 1.7.10's `renderArmHoldingItem` opens the same translate written as `0.7f * 0.8f, -0.65f * 0.8f - (1 - equip) * 0.6f, -0.9f * 0.8f` — the same numbers with the `0.8f` constant-folded, `-0.9f * 0.8f` folding to exactly `-0.71999997f` in float. The three rotations after it (`sin(p*p*PI) * -20`, `sin(sqrt(p)*PI) * -20`, `sin(sqrt(p)*PI) * -80`, about Y, Z and X) are the same expressions on the same axes in both. There is no 1.7 swing arc to restore. What players remember as \"the 1.7 swing\" is the **held-item render pipeline**: 1.8 replaced fixed sprite quads with `BakedModel` plus the JSON `firstperson` transform block, which changed where the item sits in the hand and how it is lit. That is a renderer rewrite — an L on §9's scale, and its own project if it is ever wanted — and the one thing this mod must not do is smuggle it in under the word `swing`. A checkbox called `swing` in a mod called Old animations is a promise to do exactly that.\n\n**A mapping trap, written down because it will otherwise cost somebody an afternoon.** Legacy Fabric yarn names `ModelPart`'s **rotation** fields `posX` / `posY` / `posZ`; `ModelPart.render` feeds them to `GlStateManager.rotate` and the translation is the separate `pivotX/pivotY/pivotZ`. So `rightArm.posY = -0.5235988f` is a yaw of −30° (−π/6 to the last digit), not a position. Read it as a position and the third-person half of `block_hit` is unimplementable.\n\n**The one real revert, in two render paths.** First person, 1.8.9 `renderArmHoldingItem`: while `getItemUseTicks() > 0` every `UseAction` branch calls `applyEquipAndSwingOffset(equip, 0.0F)` — the swing progress is thrown away — and only the not-using path passes it live. 1.7.10 has the *same* shape for the swing **translate**: its three sines sit inside the `else`, so 1.7 skipped them while an item was in use exactly as 1.8.9 does. But the equip-and-swing block that follows is **outside** that branch in 1.7 and re-reads `getHandSwingProgress(tickDelta)` live. The whole difference is therefore one float argument. An earlier reading of this had the fix as \"also call `translateSwingProgress`\"; that would overshoot 1.7 by precisely the translate 1.7 also skipped, and `block_hit`'s own description carries the warning. `applySwordBlockTransformation`'s constants are identical in both versions and are not touched by anything here.\n\nThird person, `BiPedModel.setAngles`: 1.7 tests `rightArmPose != 0`, applies the pitch line and then zeroes `rightArm.posY`; 1.8 switches on the pose and, in case 3 (BLOCK), adds `rightArm.posY = -0.5235988f` after the same pitch line. The revert is that one assignment, and it is folded into `block_hit` rather than given a switch of its own — the argument is in that setting's description.\n\n**One narrowing is deliberate and is not a bug.** Because 1.7's equip-and-swing block is outside the use-action branch, 1.7 applied the live swing to *every* `UseAction` — eating, drinking and bow-drawing as well as blocking. This mod reverts BLOCK only. The other three are either cancelled by the click that would have swung or do not happen inside an exchange, so a full revert would buy nothing and would put three more render states in front of the preview test. If that is ever wanted it is a wider setting, not a wider `one_seven`.\n\n**`use_while_digging` is not here.** It shipped in the sibling mod `old_input`, with `dig_while_using` and `no_miss_delay`, because all three change what the client *does* on an input and are classed `grey`; this mod is animations only and is `safe`. `hypixel_safe` is per mod, so keeping them together would have cost the badge to a player who only wanted the sword to move while blocking. That split is argued at the top of `old_input.json`, including why it is the opposite call from `overlay`.",
  "id": "old_animations",
  "kind": "gameplay",
  "category": "pvp",
  "hypixel_safe": "safe",
  "label": "Old animations",
  "icon": "reset",
  "the": false,
  "on_description": "the 1.7 animation reverts are enabled",
  "description": "Puts the 1.7 blocking animation back: the sword moves with your swing instead of freezing.",
  "settings_title": "Old animations settings",
  "settings_description": "Settings for the Old animations gameplay mod. `docs/mod-roster.md` §3.2 #1 ranks this first on the whole roster — \"the single most-noticed absence in a 1.8 PvP client\" — and the honest thing to say about the version that shipped is that it is **smaller than that row implies, because most of the row is not real**. The roster describes reverting \"swing, block-hit and item-use animation\". Disassembled against a real 1.7.10 jar, the swing is identical in the two versions down to the constants, and the item-use animation differs only in the blocking case. So there is one revert here and it is the block hit, in both the first-person and the third-person path, plus one animation that belongs to neither version and says so.\n\nThe reason that is worth stating on the settings page rather than only in the changelog: a player enables a mod called Old animations expecting their swing to change, and it will not. What they are remembering is 1.8's held-item **render pipeline** — `BakedModel` and the JSON `firstperson` transforms replacing fixed sprite quads — which is a renderer rewrite and not a setting. The `$comment` at the top of this file is the full briefing and the reason no `swing` key exists.\n\nWhat this mod is *not* allowed to grow into is the other half of the withdrawn draft. `use_while_digging` and the two switches found beside it live in `old_input`, which is `grey`; a mod that reverts an animation and a mod that changes what a click does are different claims about the same word \"1.7\", and only one of them is free.",
  "source": "HeldItemRenderer#renderArmHoldingItem and BiPedModel#setAngles",
  "enabled": false,
  "defaults": {
    "block_hit": "one_seven",
    "swing_during_delay": false
  },
  "settings": {
    "block_hit": {
      "description": "Which version's blocking animation is drawn. `one_seven` is the mod and is the default: while you are holding right-click with a sword, your swing still moves the sword. 1.8.9 discards it — `renderArmHoldingItem`'s BLOCK branch calls `applyEquipAndSwingOffset(equip, 0.0F)`, hard-coding the swing progress to zero — so a 1.8 block-hit is a frozen arm with a hit landing somewhere behind it, and that stationary sword is the thing 1.7 players say the client \"feels wrong\" for. The fix is that one argument: pass the live `getHandSwingProgress(tickDelta)` instead of `0.0F`. **Do not also call `translateSwingProgress`** — 1.7 skipped that translate while an item was in use exactly as 1.8.9 does, so adding it back overshoots 1.7 rather than restoring it. `one_seven` also drops the −30° right-arm yaw (`rightArm.posY = -0.5235988f`) that 1.8 added to the blocking pose in `BiPedModel.setAngles`, which is how *other* players' blocking looks on your screen — your own body is drawn by their client, so nothing about how you appear to anyone else changes. That third-person half is folded in here rather than given a switch of its own on purpose: it is the same revert in the other render path, no player wants 1.7 blocking in their hands and 1.8 blocking on the model in front of them, and a boolean whose entire visible effect is a 30° arm rotation on an entity would have to be drawn in `test/preview.test.tsx` or exempted from it. If it is ever split, this sentence is the split list. `vanilla` leaves both alone and is what a player picks to compare.\n\nOne interaction worth knowing, because neither mod's page can show it: with `old_input.use_while_digging` off — its factory state — 1.8.9's `doUse` guard discards every right click made while you are mining, so you cannot *raise* the sword mid-break at all, and this setting has nothing to draw for that click. The two mods are separate on purpose (one is animation and `safe`, the other is input and `grey`), which is exactly why the dependency has to be written down rather than inferred from sitting next to each other in the grid.",
      "type": "string",
      "enum": [
        "vanilla",
        "one_seven"
      ],
      "default": "one_seven"
    },
    "swing_during_delay": {
      "description": "Whether your arm still swings during the half-second of dead time 1.8 imposes after a click that hit nothing. **Read the second half of this before assuming what it does.** On a miss, 1.8.9's `doAttack` sets `attackCooldown = 10` in survival, and for those ten ticks every further click returns at the top of the method: no swing, no attack, nothing. 1.7.10's switch has no MISS entry at all and falls straight to `return`, so a 1.7 player clicking into open air sees an arm that keeps up with the mouse. Turning this on reproduces `LivingEntity.swingHand()`'s body — the two public fields `handSwinging` and `handSwingTicks` — **without** `ClientPlayerEntity.swingHand()`'s outbound `HandSwingC2SPacket`. Nothing reaches the server and nothing about the fight changes.\n\nSo be plain about what is restored and what is not: **the click is still swallowed.** A left click inside the dead window does not attack, does not start mining and is not sent anywhere, whether this is on or off. Only the animation comes back. That means this is not a 1.7 revert either — in 1.7 the click also *worked* — it is a third behaviour that existed in no version, and the honest description of it is \"the arm agrees with the mouse\". Off by default for exactly that reason: an arm that swings on a click the game ate can be read as a hit that landed. The half that actually restores 1.7's click cadence is `old_input.no_miss_delay`, and it is in a `grey` mod because it changes how many swing packets leave the client, which is the number a CPS-based anticheat is watching. Note for whoever writes the mixin: `getMiningSpeedMultiplier()` is **private** on `LivingEntity`, so the re-entry guard needs an `@Invoker` rather than a straight copy.",
      "type": "boolean",
      "default": false
    }
  }
}
