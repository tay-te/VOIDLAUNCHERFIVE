{
  "$comment": "The properties every mod of a given `kind` carries, merged into each mod's own settings by `schema/build.mjs`. Declared once here so the ninth HUD widget cannot invent its own spelling of `scale`, and so a property added to the set reaches every HUD mod at once instead of being retrofitted into twenty settings pages. `{noun}` in a description is replaced with the mod's own `noun`.",
  "hud": {
    "$comment": "Shared by every `kind: hud` mod \u2014 the eight that own a draggable HUD item.\n\nThe first three are the set VOID already had. The three after them are the HUD chrome block, and they exist because a HUD readout is drawn over *arbitrary game pixels*: a chip that is legible on a stone wall can be unreadable on snow or on a nether ceiling, and until now the only answers a player had were `scale` and `opacity`, neither of which is about contrast.\n\nWhat this block is deliberately NOT: Lunar gives essentially every HUD mod a background colour, a border colour and per-element text colours. `design/quiet-cell-system.md` \u00a71 puts exactly that on the far side of its line \u2014 'a chip background, border or label colour per mod' is named there as what stays out, because colour in this system encodes a value or a state and is never a preference. So every property here is *structural*: on/off, or a step on a system-owned token scale. The player chooses whether a chip has a ground, never what colour that ground is. \u00a71's own list of what customisation legitimately is \u2014 'position, anchor and offset, scale, opacity, density, what is shown, format' \u2014 is the shape of this block, and `padding` is `density` under its own name.\n\nWhat is deliberately NOT in this block: `text_shadow`. It was here for one commit and removing it is the point of the note. `design/ultralight-notes.md` \u00a73 rates `text-shadow` [risky] \u2014 Ultralight's text rasteriser drops it, and on some builds it smears the glyph atlas \u2014 so the setting would have been a switch that did nothing in game, which is the exact silent failure `design/rendering-invariants.md` \u00a715 is about. \u00a73 also says the design already solves HUD legibility structurally (every readout sits on its own chip) and that the decision \"must be preserved rather than 'improved' later\". `background` above IS that chip, exposed to the player; it is the answer to legibility here, and there is no second one. \u00a73's fallback for text that genuinely cannot have a chip \u2014 a 1px outline drawn as four offset copies \u2014 is not needed, because a chip is exactly what this block offers. Do not re-add it, and do not reach for `-webkit-text-stroke` either (also unreliable in Ultralight).",
    "on": {
      "description": "Whether {on_description}.",
      "$ref": "#/definitions/on"
    },
    "scale": {
      "description": "Size multiplier of {noun}.",
      "$ref": "#/definitions/scale"
    },
    "opacity": {
      "description": "Alpha of {noun}.",
      "$ref": "#/definitions/opacity"
    },
    "background": {
      "description": "Ground drawn behind {noun}, as a step on the system's own scale rather than a colour. `subtle` is the card ground at low alpha \u2014 enough to hold a chip together over a busy texture \u2014 and it is the default because it is what every HUD readout has always been drawn on. `bare` is nothing at all: glyphs on the game, which is the vanilla treatment and is legible over sky and unreadable over snow, so it is a choice rather than a default. `solid` is the opaque card ground, for a player who wants the HUD to read as a panel. A step rather than a hex value because a per-mod background colour is what \u00a71 names as the far side of the line.\n\n**The step sets the widget's own ground; it does not paint a second one behind it.** This was `background` on the *slot*, and every widget already had a ground of its own underneath \u2014 so all three steps composited over `rgba(10,11,12,0.55)` and the visible difference between them was a two-pixel halo where the slot's padding stuck out past the chip's corner. Once density moved onto the widget the halo went and the three steps became one drawing. They resolve to `--hud-chip-bg` and `--hud-chip-bg-strong` now, the variables the chip, the editor chip and both list panels actually paint from.\n\n**`none` was renamed to `bare`, and the rename is the migration.** The old value was the default *and* it drew a ground, so it never meant what it said and no player can have chosen it deliberately: there was no way to get a bare readout at all. Every loadout on disk therefore carries `none` meaning \"I took the default\", and the honest remap is to `subtle`, which is exactly what those players have been looking at. Renaming rather than redefining is what makes that remap safe to run once and never again \u2014 a stored `none` can only have been written before this, where a redefined `none` would be indistinguishable from a player who has since chosen it. `crates/void-loadout`'s `REMAPPED_VALUES` does the remap on read; Java's `ModRegistry.clamp` already rejects an unknown enum value and keeps the default, which is the same answer arrived at for free.",
      "type": "string",
      "enum": [
        "bare",
        "subtle",
        "solid"
      ],
      "default": "subtle"
    },
    "border": {
      "description": "Whether a hairline is drawn around {noun}, at the system's own `--border-panel` alpha. Boolean rather than a colour or a width for the same reason as `background`: the edge either separates the chip from the game or it does not, and the one useful answer is already a token.",
      "type": "boolean",
      "default": false
    },
    "padding": {
      "description": "Density of {noun} \u2014 the inset between its content and its edge, as one of five steps. `density` is named in \u00a71 as legitimate customisation, and it is what a player actually means by 'make the HUD smaller' when `scale` has already made the text too small to read.\n\n**This step drives the widget's own inset, not a box around it.** For one release it set padding on the *slot* \u2014 the box `HudSlot` puts round the widget \u2014 while the widget kept its own hard-coded padding underneath. With the default `background: none` that outer box is transparent, so the setting moved an invisible edge and the drawn chip never changed size. It passed `preview.test.tsx` because the class name on the slot changed, which is exactly the erosion that file's own doc comment warns the exemption list about: a gate that compares markup cannot tell a class that draws from a class that does not. The steps now resolve to `--pad-hud-chip`, `--pad-hud-panel` and `--gap-hud-keys`, the three variables every HUD surface actually reads its density from, so the chip, the two list panels and the keycap cluster all move together and all move at every background step.\n\nFive steps rather than three because three could not say what players asked for at either end. `none` is the setting off \u2014 glyphs on the game with nothing round them \u2014 which is what a player who has already turned the ground off is after; `wide` is the panel treatment, for a HUD read at a glance across a room. `tight`, `normal` and `roomy` keep the values they had.",
      "type": "string",
      "enum": [
        "none",
        "tight",
        "normal",
        "roomy",
        "wide"
      ],
      "default": "normal"
    }
  },
  "gameplay": {
    "$comment": "Shared by every `kind: gameplay` mod. There is exactly one: a gameplay mod mutates a client-side option and draws nothing of its own, so it has no chrome, no placement and no size \u2014 `crosshair` and `hitboxes` carry their own geometry because it is the geometry of a *world* mark, not of a HUD item.",
    "on": {
      "description": "Whether {on_description}.",
      "$ref": "#/definitions/on"
    }
  }
}
